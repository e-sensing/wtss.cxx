set(PACKAGE_VERSION "@WTSS_CXX_VERSION_STRING@")

if(NOT "${PACKAGE_FIND_VERSION}" VERSION_GREATER "@WTSS_CXX_VERSION_STRING@")
  set(PACKAGE_VERSION_COMPATIBLE 1)

  if("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@WTSS_CXX_VERSION_STRING@")
    set(PACKAGE_VERSION_EXACT 1)
  endif()

endif()
